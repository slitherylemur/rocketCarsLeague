// Original: StarterPlayer/StarterPlayerScripts/cameraScript (LocalScript)
// The original script was DISABLED (Disabled = true) and its entire body was
// commented out — behaviourally a no-op, preserved below.

//local Players = game:GetService("Players")
//local camera = workspace.CurrentCamera
//local player = Players.LocalPlayer

//function SetCamera(cameraPart)

//	game.Lighting.Blur.Enabled = false
//	camera.FieldOfView = 70

//	camera.CameraType = Enum.CameraType.Scriptable

//	camera.CFrame = cameraPart.CFrame
//end

//game.ReplicatedStorage.FunctionsAndEvents.PartToCamera.OnClientEvent:Connect(function(part)
//	camera.CameraType = Enum.CameraType.Scriptable
//	camera.CFrame = part.CFrame
//end)

//game.StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.PlayerList, false)

export {};
